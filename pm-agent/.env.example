# Claude API Authentication (choose ONE option):
#
# Option 1: Direct API key (pay-as-you-go)
# ANTHROPIC_API_KEY=sk-ant-...
#
# Option 2: Claude Code OAuth token (if not using keychain)
# CLAUDE_CODE_OAUTH_TOKEN=...
#
# Option 3: Claude Code subscription (RECOMMENDED - no config needed!)
# If you're logged into Claude Code, the agent will automatically
# use your subscription token from macOS Keychain.
# Just leave both variables commented out.

# Telegram Bot (get from @BotFather)
TELEGRAM_BOT_TOKEN=123456789:ABC...
TELEGRAM_ADMIN_CHAT_ID=123456789

# Server
PORT=3001
HOST=0.0.0.0

# Agent settings
HEALTH_CHECK_INTERVAL=4h
DEEP_ANALYSIS_TIME=09:00
TIMEZONE=Europe/Moscow

# OpenRouter API (используется в Crawler, Scraper, News Analyzer)
OPENROUTER_API_KEY=sk-or-v1-...
OPENROUTER_MODEL=google/gemini-3-flash-preview

# Anthropic API (используется только в Main Agent и CLI режиме)
# ANTHROPIC_API_KEY=sk-ant-...
# CLAUDE_CODE_OAUTH_TOKEN=...

# Task Executor - автоматическое выполнение задач
# Включает автоматическое выполнение approved задач через Claude Code CLI
TASK_EXECUTOR_ENABLED=true
TASK_EXECUTOR_INTERVAL=*/5 * * * *  # Cron expression (каждые 5 минут)
TASK_EXECUTOR_REQUIRE_APPROVAL=false  # Обходить approval для коммитов

# GitHub Issues Integration - автоматическое создание GitHub Issues
# Создает GitHub Issue для каждой approved задачи с упоминанием @Claude Code
GITHUB_AUTO_CREATE_ISSUE=true  # Автоматически создавать issue при approve задачи
GITHUB_SYNC_ENABLED=true  # Включить периодическую синхронизацию с GitHub
GITHUB_SYNC_INTERVAL=15  # Интервал синхронизации в минутах
